const Loading = () => {
  return (
    <div className="flex flex-col items-center justify-center h-screen bg-white">
      <span className="loader text-blue-500" />
      <h3 className="text-sm text-neutral-800">Getting your colors ready...</h3>
    </div>
  );
};

export default Loading;
